# Vite mode for Docker: dev (default) or build
# dev = vite dev server with HMR
# build = npm run build && vite preview (production build)
# VITE_MODE=dev

# API base URL (optional; relative /api/v1 used by default)
# Override when API is on different host
# VITE_API_URL=http://localhost:8000
