# Django
SECRET_KEY=your-secret-key-change-in-production
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# Django settings module (dev for local, prod for production)
# DJANGO_SETTINGS_MODULE=config.settings.dev

# Backend run mode: runserver (default) or gunicorn
# RUN_MODE=runserver

# PostgreSQL (use POSTGRES_HOST=postgres when running in Docker)
POSTGRES_DB=therapycare
POSTGRES_USER=therapycare
POSTGRES_PASSWORD=therapycare
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CORS (comma-separated origins)
# Add http://localhost:8080 when using nginx proxy
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080
